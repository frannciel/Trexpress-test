const express = require('express');
const router = express.Router();

// Rota para a página principal
//router.get('/home', (req, res) => {
 //   res.render('home', { title: 'Minha Aplicação' });
//});

// Adicione outras rotas aqui

module.exports = router;